📊 MERN Dashboard

A full-stack dashboard built with Node.js, Express, MongoDB, and React (Vite + RTK Query).
It tracks KPIs, Products, and Transactions, providing a fast and interactive interface for viewing business metrics.
The backend exposes REST APIs, and the frontend consumes them using RTK Query for efficient state management and data fetching.